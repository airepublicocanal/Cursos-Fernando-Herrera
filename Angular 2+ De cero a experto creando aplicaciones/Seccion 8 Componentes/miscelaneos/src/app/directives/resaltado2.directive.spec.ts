import { Resaltado2Directive } from './resaltado2.directive';

describe('Resaltado2Directive', () => {
  it('should create an instance', () => {
    const directive = new Resaltado2Directive();
    expect(directive).toBeTruthy();
  });
});
