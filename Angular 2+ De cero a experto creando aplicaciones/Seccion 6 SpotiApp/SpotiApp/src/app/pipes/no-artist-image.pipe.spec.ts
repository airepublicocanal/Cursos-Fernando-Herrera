import { NoArtistImagePipe } from './no-artist-image.pipe';

describe('NoArtistImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NoArtistImagePipe();
    expect(pipe).toBeTruthy();
  });
});
