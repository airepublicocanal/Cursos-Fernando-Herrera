import { NoAlbumImagePipe } from './no-album-image.pipe';

describe('NoAlbumImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NoAlbumImagePipe();
    expect(pipe).toBeTruthy();
  });
});
